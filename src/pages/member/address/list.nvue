<!--
 * @Author: Derek Xu
 * @Date: 2023-03-25 21:54:59
 * @LastEditors: Derek Xu
 * @LastEditTime: 2023-04-19 14:35:39
 * @FilePath: \xuct-group-purchase-uniapp\src\pages\member\address\list.nvue
 * @Description: 
 * Copyright (c) 2022 by 楚恬商行, All Rights Reserved. 
-->
<!--
 * @Author: Derek Xu
 * @Date: 2023-03-22 21:40:01
 * @LastEditors: Derek Xu
 * @LastEditTime: 2023-03-25 22:33:27
 * @FilePath: \xuct-group-purchase-uniapp\src\pages\member\address\list.nvue
 * @Description: 
 * 
 * Copyright (c) 2023 by 楚恬商行, All Rights Reserved. 
-->
<template>
  <tm-app class="flex flex-col">
    <tm-navbar title="收货地址" color="#32CD32" fontColor="#fff" blur></tm-navbar>
    <tm-sheet :margin="[0, 0]" :padding="[20, 30]">
      <tm-input prefix="tmicon-search" searchLabel="搜索" focusColor="green" placeholder="按名字或电话搜索" @search="onSearch"></tm-input>
    </tm-sheet>
    <view class="flex flex-row flex-row-center-center" v-if="addressList.length === 0 && !load">
      <tm-result :showBtn="false" status="empty" color="green" subTitle="请稍后重试" title="暂无数据" />
    </view>
    <view class="list" v-else>
      <tm-sheet v-for="(item, index) in addressList" :key="index" :round="3" :shadow="2" :margin="[10]" @click="onSelect(item.id ?? '')">
        <view class="flex flex-row flex-row-center-between pa-10">
          <view>
            <view class="flex flex-row flex-row-center-between mb-10">
              <tm-tag color="green" size="s" label="默认" v-if="item.firstChoose === 1" :margin="[0, 0, 10, 0]"></tm-tag>
              <tm-text :font-size="30" :label="`${item.provinceName}${item.cityName}${item.countyName}`"></tm-text>
            </view>
            <tm-text :font-size="28" :label="item.detailInfo"></tm-text>
            <tm-text :font-size="28" :label="`${item.telNumber}${item.userName}`" class="mt-10"></tm-text>
          </view>
          <tm-icon name="tmicon-edit" @click="onEdit(item.id ?? '')"></tm-icon>
        </view>
      </tm-sheet>
    </view>

    <view class="button-bottom fixed l-0 b-0" :style="{ height: _totalBarHeight + 'rpx' }">
      <view class="relative flex flex-row flex-around flex-row-center-between bottom pa-10">
        <view class="flex flex-1 ma-10">
          <tm-button block text @click="chooseaddress" :round="25" :padding="[0, 10]" label="微信导入"></tm-button>
        </view>
        <view class="flex flex-1 ma-10">
          <tm-button block color="green" :padding="[0, 10]" :round="25" label="新增" @click="toAdd"></tm-button>
        </view>
      </view>
    </view>
  </tm-app>
</template>
<script lang="ts" setup>
import tmApp from '@/tmui/components/tm-app/tm-app.vue'
import tmNavbar from '@/tmui/components/tm-navbar/tm-navbar.vue'
import tmButton from '@/tmui/components/tm-button/tm-button.vue'
import tmResult from '@/tmui/components/tm-result/tm-result.vue'
import tmSheet from '@/tmui/components/tm-sheet/tm-sheet.vue'
import tmIcon from '@/tmui/components/tm-icon/tm-icon.vue'
import tmInput from '@/tmui/components/tm-input/tm-input.vue'
import tmText from '@/tmui/components/tm-text/tm-text.vue'
import tmTag from '@/tmui/components/tm-tag/tm-tag.vue'
import { onLoad } from '@dcloudio/uni-app'
import { computed, reactive, ref } from 'vue'
import { userAddressList, saveUserAddress } from '@/api/user'
import { loading, hideLoading } from '@/utils/dialog'

import { useAppHook } from '@/store'

const load = ref(true)
const addressList = reactive<User.Address[]>([])
const searchValue = ref('')
const inType = ref('')

const appStore = useAppHook()

const _totalBarHeight = computed(() => {
  const safeBottom = appStore.getSafeBottom()
  return uni.$tm.u.torpx(safeBottom > 0 ? 80 : 60 + safeBottom)
})

onLoad((ops: any) => {
  const { type } = ops
  if (type) {
    inType.value = type
  }
  _load('')
})

const _load = (_search: string) => {
  addressList.length = 0
  load.value = true
  loading()
  userAddressList(_search)
    .then((res) => {
      addressList.push(...res.data)
      load.value = false
    })
    .catch((err) => {
      console.log(err)
      load.value = false
    })
    .finally(() => {
      hideLoading()
    })
}

const chooseaddress = (e: any) => {
  uni.chooseAddress({
    success: (res: User.Address) => {
      if (res.errMsg == 'chooseAddress:ok') {
        const { errMsg, ...address } = res
        saveUserAddress(address)
          .then(() => {
            _load('')
          })
          .catch((err: any) => {
            console.log(err)
          })
      }
    },
    fail: (e: any) => {
      console.log(e)
    }
  })
}

const onEdit = (id: string) => {
  uni.navigateTo({
    url: './index?id=' + id,
    success: () => {
      uni.$once('address:add::success', (data) => {
        _load('')
        console.log('新增成功返回')
      })
    }
  })
}

const toAdd = () => {
  uni.navigateTo({
    url: './index',
    success: () => {
      uni.$once('address:add::success', (data) => {
        _load('')
        console.log('新增成功返回')
      })
    }
  })
}

const onSearch = (e: any) => {
  debugger
  _load(e)
}

const onClear = () => {
  searchValue.value = ''
  _load('')
}

const onSelect = (addressId: string) => {
  if (inType.value !== 'order') {
    return
  }
  uni.$emit('address:selected::success', {
    address: addressList.find((x) => x.id === addressId)
  })
  uni.$tm.u.routerTo('', 'navigateBack')
}
</script>
<style>
.list {
  flex: 1;
  padding-bottom: 90px;
}
</style>
